export function ServerLoader({height}) {
    return (
        <div className="flex flex-col align-center justify-center" style={{fontSize: "3rem",width: "100%",minHeight: height}}>
            🚀 Загрузка...
        </div>
    )
}